language: julia
os:
    - linux
    - osx
julia:
    - 0.7
    - 1.0
    - nightly
addons:
  apt:
    packages:
        - hdf5-tools
notifications:
    email: false
after_success:
    - julia -e 'cd(Pkg.dir("MAT")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
